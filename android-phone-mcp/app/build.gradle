plugins {
    id 'com.android.application'
}

android {
    namespace 'dev.speedstu.phonemcp'
    compileSdk 35

    defaultConfig {
        applicationId 'dev.speedstu.phonemcp'
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName '1.0'
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    packagingOptions {
        jniLibs {
            useLegacyPackaging true
            keepDebugSymbols += ['**/libcloudflared.so']
        }
    }
}
